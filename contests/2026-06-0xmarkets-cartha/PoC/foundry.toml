[profile.default]
src = "src"
test = "test"
out = "out"
libs = ["lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200

# For fork-based PoCs, set the RPC in your env and reference it in the test:
#   export FORK_URL="https://..."
# [rpc_endpoints]
# mainnet = "${FORK_URL}"
